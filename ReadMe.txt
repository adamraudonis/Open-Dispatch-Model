How To Run:
Type cd (space) then drag in the Code folder in Open-Dispatch-Model
Type python main.py

To run different scenarios, modify the parameters in main.py

When you are running for the first time, run python main.py -r a
This will load in all the excel files such as coal prices, etc.

When you are running later and haven’t modified the load forecasts, or prices, you can run  only python main.py which will be much faster